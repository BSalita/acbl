# Junctions to shared libraries (each has its own repo; see mklinks.bat)
mlBridge/
acbllib/
chatlib/
streamlitlib/

# Secrets
.env

# Environments / caches
.venv/
__pycache__/
.ipynb_checkpoints/
.pytest_cache/

# Run artifacts
logs/
validation_output/

# Stage-1b parquets staged for OneDrive sync to production (see u.bat);
# 2.7 GB of data, never committed
club_results_parquet/
# Local session-details cache written by acbl_club_api_service.py
club-results/
# Recent archive slice staged for OneDrive sync to production (see u.bat)
club-results-recent/

# Retired scripts (pre-version-control archive)
old/
